{
  "name": "my-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.11.4",
    "@testing-library/react": "^11.1.0",
    "@testing-library/user-event": "^12.1.10",
    "apexcharts": "^3.33.1",
    "axios": "^0.24.0",
    "bootstrap": "^5.1.3",
    "history": "^5.1.0",
    "react": "latest",
    "react-bootstrap": "^2.0.3",
    "react-dom": "latest",
    "react-router-dom": "^5.3.0",
    "react-scripts": "latest",
    "recharts": "^2.1.8",
    "web-vitals": "^1.0.1"
  },
  "scripts": {
    "start": "cp -rf ../src/json public/assets && react-scripts start",
    "build": "react-scripts build",
    "build-deploy": "cp -rf ../src/json public/assets && react-scripts build && npm run build && aws s3 sync build s3://xalians.com",
    "deploy": "npm run build && aws s3 sync build s3://xalians.com",
    "copy-json": "cp -rf ../src/json src",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
