# Art pipeline log

Running record of every action on the species art pipeline: what was tried, the exact settings, what came out, and the verdict. Kept so the work survives context resets. Newest entries at the bottom of each section. Append, never rewrite history; correct a wrong entry with a dated note under it.

## Where things are

- Plan: `docs/design/xalian-art-pipeline.md` (phases, gate, assumptions). Codex audit of it: `docs/design/xalian-art-pipeline-codex-audit.md`.
- Generator (Z-Image-Turbo, from run 10): `scripts/art/generate-zimage.py` (`unsloth/Z-Image-Turbo-unsloth-bnb-4bit`, 6.4 GB pre-quantized 4-bit, model CPU offload, prose prompt through a Qwen3 text encoder with a 512-token window, 8 steps, guidance 0, no negative prompt).
- Generator (FLUX.1 schnell, runs 12 and 13): `scripts/art/generate-flux.py` (`magespace/FLUX.1-schnell-bnb-nf4`, ungated 4-bit diffusers layout, 12.5 GB; the official repo is gated). Same prose brief as the Z-Image script.
- LoRA training: `C:\dev\src\xalians-art\ai-toolkit` (ostris/ai-toolkit, own venv), dataset `C:\dev\src\xalians-art\lora\dataset` (29 silhouettes with captions, trigger word `xalsil`), config `C:\dev\src\xalians-art\lora\xalsil-zimage.yaml`, output `C:\dev\src\xalians-art\lora\output`.
- Generator (SDXL, runs 1 to 9): `scripts/art/generate-silhouettes.py` (SDXL base 1.0 fp16, fp16-fix VAE, model CPU offload, optional IP-Adapter ViT-H with reference images, per-image manifest with seeds, versions, memory and hashes, contact sheet).
- Environment: `C:\dev\src\xalians-art\.venv` (Python 3.12.13, torch 2.11.0+cu128, diffusers, transformers, accelerate, safetensors, pillow). GPU: RTX 5070 laptop, 8 GB, driver 595. Models cached by Hugging Face under the user profile; no token needed for anything used so far.
- Outputs: `C:\dev\src\xalians-art\out\<species>\<tag>\` with `manifest.json`, one PNG per seed, `<species>-<tag>-contact.png`. Logs of each run in `C:\dev\src\xalians-art\<tag>.log`.
- Tag naming (fixed 2026-09-03 after Nick flagged inconsistent version names): `runNN-<technique>-<refs>`, for example `run04-styleblocks-xylum-thirstaserp`. NN is the global run number from the table below, never reused, across all species. Runs 2 and 3 were renamed on disk to `run02-prompt-only` and `run03-ipadapter-newtapede-thirstaserp`.
- Reference art: `docs/species-templates/art/<key>.png` (renders of Nick's SVGs), species records in `docs/species-templates/<key>.json`.
- Branch: `data/ability-catalog` in the worktree `C:\dev\src\xalians-catalog`. Frackworm (species 00030, no art yet) lives only on this branch; PR #73 merges everything else to main.

## How to run

From the worktree root:

```
PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True C:/dev/src/xalians-art/.venv/Scripts/python.exe scripts/art/generate-silhouettes.py <key> --n 8 --seed <s> --size 896 --guidance 6 [--ref key1,key2 --ip-scale 0.6 --ip-blocks all|style|style-layout] [--style ink|bold] --tag <tag>
```

About 135 to 145 s for eight images at 896 px. Several runs in one go: copy `C:\dev\src\xalians-art\run-batch-0903a.sh` (serial queue, one log per tag) and run it in the background. Run one job at a time on the GPU. Do not run at 1024 (memory fault, see run 1). Send the contact sheet to Nick with SendUserFile and record the verdict here.

## Standing rules for this work

- Local only, no paid image APIs (Nick's billing rule: stop and warn before anything that bills).
- Nick judges every sheet; the pipeline delivers candidates, he finishes by hand. The gate: at least one candidate he would accept as a starting point, on more than one species, judged with a rubric (anatomy accuracy, silhouette readability, style distance, cleanup time, crop and margins, success rate).
- Every run gets a tag, fixed seeds, and an entry below. A run with no entry did not happen.
- Commit script changes on `data/ability-catalog` without a co-author trailer (guard-commit hook).
- Nick (2026-09-03): keep the whole setup (venv, models, scripts) even if the silhouette track fails; he expects to use it for backgrounds, textures and sprites. Exhaust local models before any paid frontier API. He would rather ship no creature art than abstract renditions.
- Nick (2026-09-03, after runs 10 and 11): 'definitely moving in the right direction'; keep poking locally before anything paid; try FLUX in the same harness; go on the style LoRA. Training runs on his GPU only; Claude tokens are spent only on orchestration.
- Nick's verdict on the SDXL track (2026-09-03, after run 9): not going to reach his bar with this model; it is not a prompt problem. SDXL runs are paused, not deleted.

## Run history

| # | Date | Species | Tag | Settings | Result | Verdict |
|---|---|---|---|---|---|---|
| 1 | 2026-09-02 | frackworm | (pre-tag) | SDXL base, stock VAE, prompt only, 1024 px, seed 1000 | One flat grey image, then CUBLAS_STATUS_INTERNAL_ERROR | Fail, mechanical: stock SDXL VAE overflows in fp16 (grey output); 1024 px exceeds 8 GB with model offload |
| 2 | 2026-09-02 | frackworm | (pre-tag) | SDXL base, fp16-fix VAE, prompt only, 896 px, seeds 1000 to 1007, guidance 7, old "clean vector logo" prompt | Eight images, sheet at `out/frackworm/frackworm-contact.png` | Fail on style: engraving textures, several objects per frame, spirals, no drill head. Nick shown; my verdict none usable |
| 3 | 2026-09-03 | frackworm | run03-ipadapter-newtapede-thirstaserp (was `ip-adapter`) | SDXL base, fp16-fix VAE, IP-Adapter sdxl_vit-h, refs newtapede + thirstaserp, ip-scale 0.6, 896 px, seeds 2000 to 2007, guidance 6, rewritten spatial prompt | Eight images, sheet at `out/frackworm/ip-adapter/frackworm-ip-adapter-contact.png`, manifest complete | Style much closer (single creature, solid black, white incisions); content leakage from the references (legs, antennae, snake head), no drill head. Nick's read 2026-09-03: correct black-and-white style, closer to vectorized, not there yet. Fail on the style gate |
| 4 | 2026-09-03 | frackworm | run04-styleblocks-xylum-thirstaserp | IP-Adapter on style blocks only (up block 0) at 0.8, refs xylum + thirstaserp (both limbless), `--style bold` prompt (heavy masses, thick contour, die-cut sticker, stencil), drill head first in the body phrase, body brief in encoder 1 only, seeds 4000 to 4007 | Eight images, 757 s (shared the GPU with a stray run 5 for ten minutes) | Style: bolder masses, the most vector-like so far (4002, 4003, 4007). Content: wrong in every tile (legs, scorpion, dragons, one human figure); no worm, no drill; grey backdrops in five tiles. Fail |
| 5 | 2026-09-03 | frackworm | run05-fullscale04-xylum-thirstaserp | IP-Adapter on all blocks at 0.4, same refs, bold prompt, body in encoder 1 only, seeds 5000 to 5007 | Eight images, 140 s | Content: most serpentine of the three (5001, 5002, 5005, 5007 are limbless bodies) but faces, tendrils, no drill; grey backdrops, glows, one frame. Style: brushier than run 4. Fail, but the closest body so far |
| 6 | 2026-09-03 | thirstaserp (known-species control) | run06-control-styleblocks-newtapede-xylum | Style blocks at 0.8, refs newtapede + xylum, bold prompt, seeds 6000 to 6007, body in encoder 1 only; judged against `art/thirstaserp.png` | Eight images, 132 s | Anatomy failed outright: six quadrupeds (dog, lizard, rodent), two coiled shapes without hood or fangs, grey backdrops. Diagnosis: with the body brief only in encoder 1 and the adapter on the style blocks at 0.8, the model does not hold the species. Fail on the control, which is the useful result: it pins the fault on the prompt split, not on the species |
| 7 | 2026-09-03 | frackworm | run07-bothenc-full05-xylum-thirstaserp | Body brief in both encoders with the compact style (one 66-token prompt), per-species negatives (legs, arms, paws, claws, fur, face, human), negatives for grey background, glow, vignette; full adapter at 0.5, refs xylum + thirstaserp, seeds 7000 to 7007 | Eight images, 135 s | Content fixed by the encoder change: every tile is limbless and serpentine, 7000 is a segmented worm, 7003 a ringed cobra (thirstaserp leakage). Still no drill head in any tile. Style: 7000 and 7003 bold and vector-like; 7004 to 7007 tangles of tendrils (xylum leakage); one grey backdrop (7001). Fail on anatomy (drill), closest yet on style |
| 8 | 2026-09-03 | thirstaserp (control) | run08-control-bothenc-full05-newtapede-xylum | Same settings as run 7, refs newtapede + xylum, seeds 8000 to 8007 | Eight images, 141 s | Control passes on species: 8006 and 8007 are hooded serpents with fangs and ringed bellies, 8004 and 8005 fanged serpents; no quadrupeds. Against Nick's original: bodies are tangled with extra tendrils (xylum leakage), 8000 and 8001 grey backdrops, none has the flower. Style of 8006 and 8007 is in the right register, cleanup would be heavy |
| 37 | 2026-09-03 | thirstaserp (control) | run37-control-schnell-poses-8 | FLUX.1 schnell, pose list, 8 seeds | 8 images | Varied poses, strong hoods; 37005, 37007 near the set; one detached head, one stray mark, grey scales on two |
| 38 | 2026-09-04 07:42 | thirstaserp | run38-klein-showcase-16 | FLUX.2 klein 4B, showcase brief, one prompt, 16 seeds | 16 images | The showcase layer works: every seed rears with hood, open jaws on two fangs with a venom drop, tail up with a beaded rattle and motion arcs. Heads still dragon-like. Picks 38000, 38002, 38005, 38007, 38010, 38011, 38013. Best Thirstaserp sheet of the program |
| 39 | 2026-09-04 07:44 | frackworm | run39-klein-showcase-16 | FLUX.2 klein 4B, showcase brief, one prompt, 16 seeds | 16 images | Three-wedge drill jaws on every tile, sand jets and rock chips at the head, faceless on 13 of 16 (eyes on 39004, 39007, 39012). Vents read as head spray, drill cone is a plain spike, scale not conveyed. Picks 39005, 39006, 39009, 39011, 39013, 39015 |
| 40 | 2026-09-04 07:47 | thirstaserp | run40-schnell-showcase-12 | FLUX.1 schnell, showcase brief, 12 seeds | 12 images | Style lost: full-color cobras on backdrops. Cause: 360 T5 tokens against a 256 cap, trailing style clause truncated. Anatomy right. Rerun as 42 |
| 41 | 2026-09-04 07:57 | frackworm | run41-schnell-showcase-12 | FLUX.1 schnell, showcase brief, 12 seeds | 12 images | Same truncation (407 tokens): color concept renders of a drill worm in desert rock. Rerun as 43 |
| 42 | 2026-09-04 08:22 | thirstaserp | run42-schnell-showcase-stylefirst-12 | FLUX.1 schnell, showcase brief, style first, T5 512, 12 seeds | 12 images | Style back, brief lost: closed mouths, tongues, no fangs, rattle absent or detached, two-headed tiles. Schnell cannot hold a 360-token brief |
| 43 | 2026-09-04 08:32 | frackworm | run43-schnell-showcase-stylefirst-12 | FLUX.1 schnell, showcase brief, style first, T5 512, 12 seeds | 12 images | Eyed heads on 10 of 12, beaks, black mass on 43005, no drill or vents. The showcase brief is a klein tool; schnell stays for short briefs only |
| 44 | 2026-09-04 08:54 | thirstaserp | run44-klein-showcase2-16 | klein, showcase v2 wording (cobra wedge head), 16 seeds | 16 images | Head fix landed: wedge heads, hood, fangs, raised rattle on every seed. Best Thirstaserp sheet so far. Picks 44000, 44001, 44002, 44003, 44006, 44011, 44012, 44013 |
| 45 | 2026-09-04 08:56 | frackworm | run45-klein-showcase2-16 | klein, showcase v2 wording (screw-thread cone, four vent holes), 16 seeds | 16 images | Screw cones on 45000, 45003, 45010, 45013; the vent-hole wording put eye-like circles on 10 of 16. Revise wording |
| 46 | 2026-09-04 08:59 | thirstaserp | run46-klein-showcase2-g25-8 | klein, v2 wording, guidance 2.5, 8 seeds | 8 images | Same as 44, smoother, more uniform, no gain. Keep guidance 1 |
| 47 | 2026-09-04 09:00 | frackworm | run47-klein-showcase2-g25-8 | klein, v2 wording, guidance 2.5, 8 seeds | 8 images | Same eye problem as 45, no gain |
| 48 | 2026-09-04 09:02 | thirstaserp | run48-klein-showcase2-refs-newtapede-drilltail-8 | klein, v2 wording, reference images newtapede + drilltail at 768, 8 seeds | 8 images | Style toward the set (heavier masses, bolder hood) but drilltail rings leaked into the tail on five seeds. References steer style and leak anatomy |
| 49 | 2026-09-04 09:04 | frackworm | run49-klein-showcase2-refs-thirstaserp-drilltail-8 | klein, v2 wording, reference images thirstaserp + drilltail, 8 seeds | 8 images | Best drill heads of the program: screw cones with three jaws, jets, chips, faceless on 5 of 8. Picks 49000, 49001, 49006, 49007 |
| 50 | 2026-09-04 09:09 | frackworm | run50-klein-showcase3-refs-thirstaserp-drilltail-16 | klein, showcase v3 wording (slot nozzles), refs thirstaserp + drilltail, 16 seeds | 16 images | Faceless on all 16, screw cones and jaws, jets, chips. Best Frackworm sheet of the program. Picks 50002, 50004, 50007, 50008, 50010, 50013, 50015 |
| 51 | 2026-09-04 09:13 | thirstaserp | run51-klein-showcase2-refs-graviclaw-yetimoth-8 | klein, showcase v2 wording, refs graviclaw + yetimoth, 8 seeds | 8 images | Leakage gone, set-like weight, all signatures on every seed; single-ball rattle on three. Picks 51000, 51002, 51003, 51007 |
| 52 | 2026-09-04 10:02 | frackworm | run52-klein-fghead-refs-thirstaserp-drilltail-16 | klein, foreground-head pose (words), refs thirstaserp + drilltail, 16 seeds | 16 images | Overshot: spiked-ball or coiled-shell heads two to three times the body width. Not usable |
| 53 | 2026-09-04 10:07 | frackworm | run53-klein-fghead-refs-50000-50004-12 | klein, foreground-head pose, refs = tiles 50000 + 50004 with a take-head-from-first, body-from-second note, 12 seeds | 12 images | Klein composed the two tiles as asked on every seed: 50000 head, 50004 body, mild depth, near-identical tiles. Clean: 53001, 53002, 53003, 53007, 53008, 53010 |
| 54 | 2026-09-04 10:11 | frackworm | run54-klein-fghead-refs-50000-50004-1536-4 | klein, run 53 recipe at 1536 px, 4 seeds | 4 images | Design holds, cut lines and jets survive the trace, peak 5.7 GB, 37 s a tile. 53002 and 53003 cleanest |
| 55 | 2026-09-04 10:14 | frackworm | run55-klein-fghead-refs-50000-50004-2048-2 | klein, run 53 recipe at 2048 px, 2 seeds | 2 images | Holds, but peak 9.9 GB spills past the card into shared memory, 83 s a tile. Do not use 2048 on this laptop |
| 56 | 2026-09-04 10:20 | frackworm | run56-klein-depth-refs-50000-50004-8 | klein, depth pose, refs = tiles 50000 + 50004, 8 seeds | 8 images | References win, run 53 composition again, tiny tail nub literalized. No depth |
| 57 | 2026-09-04 10:22 | frackworm | run57-klein-depth-ref-headcrop-8 | klein, depth pose, ref = crop of the 50000 head, 8 seeds | 8 images | The depth arrives: head at half to two thirds, body tapering behind. Head drifted, body short. Best 57000, 57001, 57004, 57007 |
| 58 | 2026-09-04 10:24 | frackworm | run58-klein-depth-refs-thirstaserp-drilltail-8 | klein, depth pose, set refs, 8 seeds | 8 images | Recession works but the head turns coiled-shell. Pose reference only |
| 59 | 2026-09-04 10:28 | frackworm | run59-klein-depthlong-refs-headcrop-50004-12 | klein, depth-long-body pose, refs = head crop + body tile 50004, 12 seeds | 12 images | Body tile locks the run 53 layout again; detached tail worm on two. Whole tiles always fix the composition |
| 60 | 2026-09-04 10:32 | frackworm | run60-klein-depthlong-ref-headcrop-12 | klein, depth-long-body pose, ref = head crop alone, 12 seeds | 12 images | The depth: head large, body shrinking to a point (60005, 60006, 60007, 60009). Head drifted to a spike crown; three split bodies |
| 61 | 2026-09-04 10:36 | frackworm | run61-klein-depthlong-ref-headcrop1536-jaws-12 | klein, depth pose + three-jaws clause, ref = head crop from the 1536 tile, 12 seeds | 12 images | Jaws back as three wedges, head at half to two thirds, body receding to a point. Clean: 61002, 61004, 61007, 61010, 61011. The pick set |
| 62 | 2026-09-04 12:07 | frackworm | run62-klein-v4-plates-eyes-slurry-refs-16 | klein, v4 brief (plates, stalk eyes, slurry first), default pose, set refs, 16 seeds | 16 images | Eyes and slurry land, the creature reads as a character; head turned into a hose fitting pouring from the front, drill lost. Wording fix for run 64 |
| 63 | 2026-09-04 12:12 | frackworm | run63-klein-v4-depth-slurry-refs-12 | klein, v4 brief, depth-slurry pose, set refs, 12 seeds | 12 images | Thirstaserp leak: cobra heads spewing slurry. Set refs dropped for depth poses |
| 64 | 2026-09-04 13:03 | frackworm | run64-klein-v4b-drill-collar-refs-16 | klein, v4b wording, set refs, 16 seeds | 16 images | Drill, stalk eyes, collar ports and slurry together on about half the seeds (64007, 64010, 64015); the rest still make the front a spout. First sheet with all three |
| 65 | 2026-09-04 13:08 | frackworm | run65-klein-v4b-drill-collar-norefs-12 | klein, v4b wording, no refs, 12 seeds | 12 images | Thinner, dusty spray, flat plates; set refs earn their place on the default pose |
| 66 | 2026-09-04 13:11 | frackworm | run66-klein-v4-depth-ref-headcrop64010-12 | klein, v4, depth-slurry pose, ref = head crop of 64010, 12 seeds | 12 images | Front end holds on every seed (cone, port ring, stalk eyes, jets), body recedes to a point. Best 66005, 66006, 66008, 66009, 66010. Depth pick set for v4 |
| 67 | 2026-09-04 13:14 | frackworm | run67-klein-v4-depth-ref-headcrop64015-12 | klein, v4, depth-slurry pose, ref = head crop of 64015, 12 seeds | 12 images | Chunkier, splatter instead of streams, teeth on two cones. 66 is the route |
| 68 | 2026-09-04 13:32 | frackworm | run68-klein-v5-pinpoint-refs-67011-set-12 | klein, v5 (pinpoint eyes), default pose, refs = 67011 head crop + set, 12 seeds | 12 images | Cleanest drill cone and port collar yet (68001, 68003, 68004, 68006, 68008, 68011); stalk eyes copied from the crop despite the brief |
| 69 | 2026-09-04 13:36 | frackworm | run69-klein-v5-pinpoint-depth-ref-67011-12 | klein, v5, depth-slurry pose, ref = 67011 head crop alone, 12 seeds | 12 images | Toothy maw at the viewer and stalk eyes copied from the crop. Reference must be cleaned first |
| 70 | 2026-09-04 13:40 | frackworm | run70-klein-v5-refs-68003-set-12 | klein, v5, default pose, refs = clean 68003 head crop + set, 12 seeds | 12 images | Clean on all twelve: cone, port collar, jets, plates; no stalks or teeth; pinpoint eyes absent. Picks 70001, 70004, 70006, 70009, 70010 |
| 71 | 2026-09-04 13:45 | frackworm | run71-klein-v5-depth-ref-68003-12 | klein, v5, depth-slurry pose, ref = clean 68003 head crop, 12 seeds | 12 images | Most consistent sheet of the program; head at half the frame, body receding; eyes absent. Picks 71001, 71005, 71006, 71010 |
| 72 | 2026-09-04 13:49 | frackworm | run72-klein-v5-eyedots-refs-68003-set-12 | klein, v5, eye dots on the cone base, default pose, refs = 68003 crop + set, 12 seeds | 12 images | A faint white eye dot on the cone base on about half the seeds (72000, 72004, 72005, 72006, 72008, 72010); otherwise run 70. Nick to judge whether the dot is enough |
| 36 | 2026-09-03 | frackworm | run36-schnell-poses-12 | FLUX.1 schnell, pose list, 12 seeds | 12 images | More character, more mess than klein; faces on four; usable 36001, 36003, 36007, 36011 |
| 35 | 2026-09-03 | thirstaserp (control) | run35-control-klein-poses-8 | FLUX.2 klein, pose list, 8 seeds | 8 images | Six distinct poses, flat black with thin cuts, clean; dragon-like heads, no rattle; 35003, 35005, 35007 picks |
| 34 | 2026-09-03 | frackworm | run34-klein-poses-12 | FLUX.2 klein, pose list, 12 seeds | 12 images, 8 s each | Pose list works: bursting, rearing, spirals, profiles; faceless drill cones on ten of twelve; clean. Best 34001, 34002, 34007, 34008, 34009 |
| 33 | 2026-09-03 | thirstaserp (control) | run33-control-flux2-klein-4b | FLUX.2 klein 4B nf4, 8 seeds, 1024, 4 steps, guidance 1 | 8 images, 8 s each, 4.8 GB peak | Flattest, cleanest vector treatment so far: solid black, thin white cuts, hood, fangs, coil, white ground on all eight; dragon-like heads, one pose. Closest to the set's register with no training |
| 32 | 2026-09-03 | frackworm | run32-flux2-klein-4b | FLUX.2 klein 4B nf4, 8 seeds | 8 images, 64 s | Most literal brief reading: tapered ringed bodies with a drill point, faceless; one pose, reads as a shell or grub |
| 31 | 2026-09-03 | thirstaserp (control) | run31-control-flux-schnell-8 | FLUX.1 schnell nf4, 8 seeds | 8 images | Hooded serpents with fangs and tongues, pose variety, no grey; 31005 and 31007 closest to the set |
| 30 | 2026-09-03 | frackworm | run30-flux-schnell-16 | FLUX.1 schnell nf4, 16 seeds | 16 images, 30 s each | Clean segmented worms; drill-like fronts on seven tiles, faces on four; best starting tiles 30003, 30013, 30015, 30006 |
| 26 | 2026-09-03 | frackworm | run26-lora2-s03 | LoRA v2 final at 0.3, full brief, 4 seeds of run 10 | 4 images | Base quality kept, flat black with white cuts; faces on three of four seeds |
| 27 | 2026-09-03 | frackworm | run27-lora2-s05 | LoRA v2 final at 0.5 | 4 images | Between runs 26 and 22; same faces |
| 28 | 2026-09-03 | thirstaserp (control) | run28-control-lora2-s03 | LoRA v2 final at 0.3, 4 seeds of run 11 | 4 images | Clean flat cobras close to run 11 quality with the grey gone; 11000, 11001 picks |
| 29 | 2026-09-03 | thirstaserp (control) | run29-control-lora2-s05 | LoRA v2 final at 0.5 | 4 images | Nearly identical to 28, a little more belly detail |
| 21 | 2026-09-03 | frackworm | run21-lora2-800-s10 | Z-Image + LoRA v2 final, strength 1.0, full brief with trigger, 4 seeds of run 10 | 4 images | Solid black with thin white segment cuts, no breakage; 10000 faceless ringed coil, 10002 drill spire with an eye, 10001 grey slabs, 10003 snake head. Two faces of four |
| 22 | 2026-09-03 | frackworm | run22-lora2-800-s07 | LoRA v2 final, strength 0.7 | 4 images | Cleanest Frackworm tiles of the program: 10000 faceless coil with ringed drill front, 10002 drill spire on a segmented body; two usable starting tiles |
| 23 | 2026-09-03 | frackworm | run23-lora2-600-s10 | LoRA v2 step 600, strength 1.0 | 4 images | Between 21 and 22; 10000 best drill spire of the three sheets |
| 24 | 2026-09-03 | thirstaserp (control) | run24-control-lora2-800-s10 | LoRA v2 final, strength 1.0, 4 seeds of run 11 | 4 images | Best Thirstaserp tiles of the program: flat black cobra, hood, fangs, white belly cuts, white ground, in the register of the set; 11001 and 11003 near finished |
| 25 | 2026-09-03 | thirstaserp (control) | run25-control-lora2-800-s07 | LoRA v2 final, strength 0.7 | 4 images | Same quality, smoother contours; passes the control in my judgment |
| 14 | 2026-09-03 | frackworm | run14-lora750-s10 | Z-Image + LoRA step 750, strength 1.0, full brief with trigger, 4 seeds of run 10 | 4 images | Bodies gain white cut lines and a hand-inked feel; two faces, one ragged tile. Closest of the six, behind run 10 on cleanliness |
| 15 | 2026-09-03 | frackworm | run15-lora1000-s10 | step 1000, strength 1.0 | 4 images | Broken: fragments, grey wash. Overtrained |
| 16 | 2026-09-03 | frackworm | run16-lora1000-s07 | step 1000, strength 0.7 | 4 images | Coherent, incision lines on every body, 10002 drill-like spire, faces on two. Usable vocabulary, messy composition |
| 17 | 2026-09-03 | frackworm | run17-lora1500-s10 | step 1500, strength 1.0 | 4 images | Same damage as run 15 |
| 18 | 2026-09-03 | frackworm | run18-lora1500-s07 | step 1500, strength 0.7 | 4 images | Like run 16, slightly cleaner ring cuts |
| 19 | 2026-09-03 | thirstaserp (control) | run19-control-lora1000-s10 | step 1000, strength 1.0, 4 seeds of run 11 | 4 images | Brushy hand-inked strokes and white cuts; 11001 the best LoRA tile (hood, coils, fangs, clean ground); more like Nick's hand than run 11, less clean |
| 12 | 2026-09-03 | frackworm | run12-flux-schnell | FLUX.1 schnell nf4, same prose brief as run 10, 1024 px, 4 steps, guidance 0, seeds 12000 to 12007 | Eight images, 270 s including load, 30 s per image, peak 10.5 GB allocated (spills into shared system memory on Windows, still runs) | Cleanest sheet yet: pure white backgrounds, crisp ring segments, no grey except two small ground marks. Drill: 12005 has a spiral-shell drill head at the front, 12002, 12003 and 12007 have pointed tips; 12000 and 12004 have simple faces. Better than run 10 on background, edge quality and drill. Sent to Nick |
| 13 | 2026-09-03 | thirstaserp (control) | run13-control-flux-schnell | Same, seeds 13000 to 13007 | Eight images, 256 s | All eight hooded fanged serpents, no grey, real pose variety, more stylized than run 11's clip-art cobras and closer to Nick's register. Still no rattle or flower. Better than run 11 on every rubric line. Sent to Nick |
| 10 | 2026-09-03 | frackworm | run10-zimage-turbo | Z-Image-Turbo 4-bit, prose brief (drill as the whole front end, no face), prose style, 1024 px, 8 steps, guidance 0, seeds 10000 to 10007 | Eight images, 180 s including model load, 22 s per image at 1024, peak 6.3 GB | Generation gap confirmed: every tile is a single solid black ring-segmented worm on white with thin white cuts, in the register of Nick's set. 10006 has a true drill point (at the tail end) and no face; 10004 has the drill at the tail and a fanged face; 10002, 10003, 10007 have simple faces; 10000 and 10005 are coiled rings. Grey ground puddles under five tiles (a threshold removes them). First sheet with a candidate I would hand Nick as a starting point (10006, mirrored so the drill leads). Sent to Nick |
| 11 | 2026-09-03 | thirstaserp (control) | run11-control-zimage-turbo | Same settings, prose Thirstaserp brief, seeds 11000 to 11007; judged against `art/thirstaserp.png` | Eight images, 190 s | Species holds in all eight: hooded cobra, open jaws with two fangs, coiled body, white background, thin white cuts. Against Nick's original: these are realistic clip-art cobras rather than his stylized design, all eight share one pose (low variety), belly scales come out grey (threshold needed), no rattle, no flower. Passes the anatomy half of the control, style is close but generic. Sent to Nick |
| 9 | 2026-09-03 | frackworm | run09-full05-thirstaserp-drilltail | Refs thirstaserp + drilltail (xylum dropped for its tendrils; drilltail carries a drawn drill), brief reworded to 'pointed screw auger drill bit with spiral threads', otherwise run 7 settings, seeds 9000 to 9007 | Eight images, 141 s | Best anatomy so far: ring-segmented single bodies in 9000, 9003, 9005, 9006. The drill now appears, but as a separate object (9005, a drill bit floating beside the worm) or fused to a machine base (9002, 9006), never as the head. Heads are snake or centipede heads with eyes. Backgrounds white except 9001. Style: 9003 and 9005 are clean and bold; 9004 and 9007 cluttered. Fail on the drill head, otherwise the closest sheet |

## Fixes and findings

- 2026-09-04 13:45 Reference beats brief: a reference crop that shows a feature the brief forbids (stalk eyes, teeth) wins every time (runs 68, 69). Clean the crop (paint out the feature) before using it, or crop from a tile that lacks it.
- 2026-09-04 12:20 Wording lesson from run 62: klein takes mechanical nouns literally. "Vent nozzles" became a hose fitting, "blunt ringed cone" a faucet. Name the drill as the front end explicitly, put the vents on a named body part (the collar segment) as ports, and say where the spray goes (sideways and back) and what does not happen (nothing pours from the tip). Session note: the background wait for runs 62 and 63 was lost when the Claude session restarted, but the batch itself completed; always check the batch log before rerunning.
- 2026-09-04 11:00 Design ruling from Nick after reading the lore against the images: the canon is thin on appearance and the images were a worm with teeth. Frackworm record amended (chitin plates, tiny stalked eyes, armored 100, `amendments` note in the record), ruling in RULINGS.md, relatable-first principle added to the creature system design section 1. Showcase entry rewritten: signature 1 is the slurry blasting from the vent ring, signature 2 the blunt ringed drill head with a small maw; eyes and plates in the visible list. The head crops from 50000 and 53002 are obsolete for this design.
- 2026-09-04 10:50 Depth lever: with whole tiles as references klein copies their composition and ignores pose words (runs 53, 56). A crop of just the part to keep (the 50000 head, `out/frackworm/ref-50000-head-crop.png`) frees the composition, and the depth pose then lands (run 57). Rule: reference the part, not the picture, when the composition must change.
- 2026-09-04 10:40 SVG polarity bug: every SVG written by `threshold-trace.py` before this fix was inverted (a frame-sized black path with the creature as a hole), because potracer treats False as foreground. The `-bw.png` previews were right, which hid it; the first vector render (`scripts/art/compare-at-size.py`, resvg) caught it. Fixed by tracing `~bw`; run 54 retraced. Rule: judge SVGs from a vector render, never from the preview PNG. `compare-at-size.py <out.png> label=path.svg ...` renders traced and hand-drawn SVGs at one height.
- 2026-09-04 10:20 Resolution ceiling: klein at 1536 px peaks at 5.7 GB and is the finishing resolution; 2048 px peaks at 9.9 GB allocated (spills to shared memory, the regime that preceded the crashes) and is off limits on this laptop. Thin cut lines trace cleanly from 1536 without upscaling.
- 2026-09-04 10:15 Tile-to-tile composition: klein's reference input takes generated tiles as well as hand-drawn art (`--refs path.png,path.png` with `--ref-note` naming what each contributes). Run 53 combined the head of one tile with the body of another in one step on every seed, which covers Nick's split-and-splice idea without Illustrator. Pose words that ask for "huge in the foreground" overshoot badly (run 52); with tile references the pose words barely matter. `threshold-trace.py` gained `--upscale`, `--alphamax`, `--opttolerance`, `--only` for the high-resolution finishing pass; `brief.py` takes a named pose from `showcase.json` `poses`.
- 2026-09-04 09:10 Klein reference input: `generate-flux2klein.py --refs a,b` passes hand-drawn silhouettes from `docs/species-templates/art/` as klein multi-reference images (768 px) with a "draw a new creature in exactly this style" preamble. No training, 8 s a tile still. Steers style toward the set and gives the best Frackworm drill heads (run 49); references leak anatomy (drilltail rings into the Thirstaserp tail, run 48), so pick references that do not share parts with the target. Guidance 2.5 gave nothing over 1.0 (runs 46, 47). Wording note: "round vent holes" on the neck draws eyes; say slots or nozzles, never round holes near the head.
- 2026-09-04 07:30 Showcase layer (Nick's ask 2026-09-04): `scripts/art/showcase.json` names per species the signature features with the visual cue that sells each, the parts also in view, the parts the pose may hide, and one fixed pose; `scripts/art/brief.py` builds the prompt and refuses unless every `physiology.anatomy` key in the record is signature, visible or hidden. Generators take `--brief showcase` (one prompt, many seeds; no pose cycling). Run `python scripts/art/brief.py <key>` to print the prompt and coverage table.
- 2026-09-04 08:35 Schnell and long briefs: with the truncation fixed (runs 42, 43) schnell keeps the style but drops the signature content (no fangs, detached rattles, eyed worm heads). Klein's Qwen3 encoder follows the 360 to 407 token showcase brief; schnell's T5 does not. Ruling for now: showcase briefs run on klein; schnell is a second opinion on short briefs only.
- 2026-09-04 08:10 Schnell truncation: the showcase prompts are 360 and 407 T5 tokens; generate-flux.py capped T5 at 256, so the style clause at the end was cut and runs 40 and 41 came out as full-color renders. Fix: style first in showcase mode and `max_sequence_length=512` (T5 maximum). Klein's Qwen3 encoder has no such cap and was unaffected. Rule: any generator with a token cap puts the style clause first and checks the count.
- 2026-09-02: `enable_vae_slicing` no longer exists on the pipeline in this diffusers; use `pipe.vae.enable_slicing()` (later removed as useless for one image per the audit).
- 2026-09-02: flat grey output = fp16 VAE overflow; `madebyollin/sdxl-vae-fp16-fix` fixes it.
- 2026-09-02: CUBLAS internal error at 1024 px = memory pressure (Codex diagnosis); 896 px with model offload runs at about 17 s per image.
- 2026-09-03: diffusers wants `ip_adapter_image=[[img1, img2]]` for several references on one adapter, not a flat list.
- 2026-09-03: the session restart killed background jobs; long runs must write their manifest incrementally (done) and the log must be updated before a clear.
- 2026-09-03: Nick's read of run 3 says the gap is vector-likeness, not black and white: his art is heavy solid masses with a few thin white cuts, run 3 was scratchy thin ink drawing. Levers tried in runs 4 to 6: a bold mass-first prompt (`--style bold`), style-only attention blocks so the adapter scale can go up without copying reference anatomy (`--ip-blocks style`), limbless references.
- 2026-09-03: **CLIP truncation.** The run 3 prompt was 100 CLIP tokens against a 77 limit, so everything after 'side or' in the style brief (contour, white incisions, flat ink, no other objects) never reached the model; run 3's style came almost entirely from the IP-Adapter. Fix: the body brief goes to SDXL's first text encoder (`prompt`), the style brief to the second (`prompt_2`), anatomy keys dropped from the brief when a hand-written body phrase exists, and the script refuses to run if any part exceeds 77 tokens. Run 4 was started with the truncated prompt, aborted, and rerun (folder `aborted-run04-truncated-prompt`).
- 2026-09-03: **Stopping a batch.** TaskStop on a background batch does not kill the batch shell or its python child on Windows; the old shell carried on to the next run while a new batch started, putting two generators on the GPU at once. To stop a batch: kill python (`taskkill //F //IM python.exe`), then the shell by command line (PowerShell `Get-CimInstance Win32_Process` filtered on `run-batch`), then confirm `nvidia-smi` shows about 220 MiB used before starting anything.
- 2026-09-03: **Prompt split lost the anatomy.** Runs 4 to 6 sent the body brief to encoder 1 only and the style to encoder 2 (OpenCLIP bigG, the stronger of SDXL's two). The control species came out as quadrupeds. From run 7 the body brief goes to both encoders (a compact style keeps the whole prompt under 77 tokens), and per-species negatives name what the model keeps adding.
- 2026-09-03: 'die-cut sticker' and 'stencil' in the style brief produced grey backdrops and glows in runs 4 to 6; dropped, with 'grey background, glow, vignette' added to the negative prompt.
- 2026-09-03: potracer (pure Python potrace) installed in the venv; a threshold plus trace trial on run 5 tile 5002 lives in `out/frackworm/trace-trial/` (threshold PNG and SVG with evenodd fill so white cuts are true holes).
- 2026-09-03: **FLUX.1 schnell 4-bit on 8 GB works** through the ungated `magespace/FLUX.1-schnell-bnb-nf4` layout with model CPU offload; 30 s per 1024 image, 10.5 GB peak allocation served partly from shared system memory. On these briefs it beat Z-Image-Turbo on background cleanliness, edge quality, variety and the drill.
- 2026-09-03: **LoRA training setup** (Sonnet agent): ai-toolkit cloned with its own venv (torch 2.13.0+cu130, diffusers pinned commit, peft, bitsandbytes, optimum-quanto); dataset of the 29 silhouettes (1000x950 RGB, no alpha) with captions from bodyPlan, anatomy and covering, trigger word `xalsil`; config `lora/xalsil-zimage.yaml` (arch zimage, quantize transformer and text encoder at qfloat8, low_vram, gradient checkpointing, batch 1, 512 and 768, rank 16, 1500 steps, lr 1e-4, adamw8bit, latents and text embeddings cached to disk, samples every 250 steps). Agent's estimate: 8 GB is marginal to insufficient for training; only a run tells. Training on Nick's GPU costs no money; Claude tokens are spent only on orchestration.
- 2026-09-03: **LoRA training fits on 8 GB.** `xalsil_zimage_lora_v1` runs with the full config (qfloat8 transformer and text encoder, low_vram, 512 and 768, rank 16): 7.7 GB in use, first step 235 s of warmup, then 6.5 to 7.6 s per step, about 3 hours for 1500 steps. Checkpoints and three sample images every 250 steps in `lora/output/xalsil_zimage_lora_v1/`. Nick's ruling: keep quality if it is viable, otherwise do what is needed; it was viable, nothing was cut.
- 2026-09-03: LoRA step 250 (09:42): checkpoint saved, rate settled at 4.1 s per step (about 1.5 h total). Samples barely changed from step 0 except the worm's front end became a screw drill (Drilltail showing through). Sample sheet `lora/output/xalsil_zimage_lora_v1/samples-sheet.png`, shown on the review artifact https://claude.ai/code/artifact/9ab953f3-8b22-4caf-b1c1-e1a4a4c68c49.
- 2026-09-03: LoRA step 500 (10:00): worm has a stacked drill spire on a ringed body, serpent gained a hood and lost scale texture, ape unchanged. Anatomy from the set is transferring before the cut style is; no white incisions yet.
- 2026-09-03: LoRA step 750 (10:17): serpent gained the rattle and fangs, worm clean drill-fronted tube, ape unchanged with creeping blue shading. Cut style still absent in the trainer's samples (trigger word only, no style prose). Decision: judge the finished checkpoint through `generate-zimage.py --lora` with the full brief before changing the training recipe. Sheet builder: `C:/dev/src/xalians-art/lora/make-sheet.py <step>`.
- 2026-09-03: LoRA step 1000 (10:35): first white incisions inside the black shapes (worm, serpent), so the cut style is starting to land, but the ape breaks into blue streaks and text: the run is beginning to damage the model at lr 1e-4. Plan: let it finish, then harness-test checkpoints 750, 1000, 1250, 1500 with the full brief at adapter scale 1.0 and 0.7; if the damage wins, retrain at lr 5e-5 with style words in the captions.
- 2026-09-03: LoRA step 1250 (10:52): samples nearly identical to step 1000 (same seeds), incisions on worm and serpent, ape still broken with text. Plateaued with damage; waiting for 1500 and the harness test.
- 2026-09-03: LoRA training finished 11:58 (TRAIN EXIT 0, 2 h 45 min, final `xalsil_zimage_lora_v1.safetensors` plus checkpoints 500 to 1250; the 250 checkpoint was pruned by the trainer's save limit). Step 1500 samples match 1000 and 1250: plateau with damage. Harness test runs 14 to 19 started 11:59 (`run-batch-0903d.sh`): Frackworm with checkpoints 750, 1000, 1500 at strength 1.0 and 0.7, Thirstaserp control with 1000, 4 seeds each shared with runs 10 and 11 so tiles compare one to one.
- 2026-09-03: **LoRA v1 verdict (12:15).** The adapter teaches the line vocabulary (white cuts, brushy contour, hand-inked feel) but lr 1e-4 for 1500 steps overtrains: full strength from step 1000 breaks images; 0.7 strength or the step 750 checkpoint keeps the vocabulary with faces and stray marks. Nothing beats run 10 or 12 for a clean starting tile yet, but the control (run 19 tile 11001) shows the direction is right. Next recipe: lr 5e-5, 800 steps, captions that name the style ('flat black silhouette with thin white cut lines'), sample with the full brief, and consider training on FLUX schnell since it gave the cleaner base.
- 2026-09-03 13:20: **Stability changes by Nick:** NVIDIA Studio driver 616.56 (was Game Ready 595), Lenovo Legion software updated, power mode Balanced to Performance, reboot. Burn-in run 20 (Z-Image, 12 images, 238 s) passed; telemetry every 5 s in `C:/dev/src/xalians-art/gpu-telemetry.csv`: max 84 C, 6.8 GB, 112 W. Training probe `xalsil-zimage-v2probe.yaml` (100 steps, 512) started 13:25 under a thermal watchdog (`gpu-watchdog.sh` kills training at 88 C held for three samples). Training rule: allowed again only on this stepped path (burn-in, probe, full run) with telemetry, per Nick's stability changes.
- 2026-09-03 20:35: **Pose variety confirmed on both bases** (runs 34 to 37). Klein follows the pose instruction on nearly every seed and stays clean and faceless; schnell follows it with more character and more faces and clutter. Next levers: canon pass on the Frackworm brief (scale cues from the record), rattle and hood wording for Thirstaserp, and a third species control.
- 2026-09-03 18:25: **Nick's read of the FLUX sheets:** run 32 (klein Frackworm) is the closest to what he wants in artwork and style, 'maybe not canonically what it should look like based on stats', a few in runs 30 and 31 not bad; klein slightly ahead on cleanliness; keep pushing both. Next: runs 34 to 37, klein and schnell with a shared pose list cycled per seed (`POSES` in generate-zimage.py, `--vary-pose`), 12 Frackworm and 8 Thirstaserp seeds each, trace pass after each.
- 2026-09-03 18:15: **FLUX.2 klein 4B works on 8 GB and is fast**: nf4 on load for transformer and text encoder, 8 s per 1024 image, 4.8 GB peak (lowest of any model here), 15 GB download. Its output is the flattest and cleanest vector-like treatment so far with thin white cuts, at the cost of pose variety (one coil, one grub pose per sheet). FLUX.1 schnell gives more variety and character at 30 s per image. Both sheets carry trace rows on the review page.
- 2026-09-03 17:45: **FLUX family sheets** (Nick: 'see what a run would look like with the flux models'). Runs 30 and 31: FLUX.1 schnell, 16 Frackworm seeds and 8 Thirstaserp seeds, trace pass applied. Runs 32 and 33: FLUX.2 klein 4B (`black-forest-labs/FLUX.2-klein-4B`, open, Apache 2.0, diffusers `Flux2KleinPipeline`, transformer and Qwen3 text encoder loaded with bitsandbytes nf4, 4 steps, guidance 1) via `scripts/art/generate-flux2klein.py`; about 15 GB download first. Review page gains a phase 'FLUX models as the base'.
- 2026-09-03 16:05: **Nick's read of LoRA v2:** style closer, quality worse; step 0 tiles are the most appealing; training paused. His question (why quality falls as style aligns) answered in chat: a 29-image adapter overwrites general drawing skill along with style, 512 training against 1024 generation, distilled turbo base fragile under fine-tuning. Two no-training experiments run: (a) threshold and trace on the base runs 10 to 13 (`scripts/art/threshold-trace.py`, Otsu, speck 60, potrace with evenodd holes; sheets in each run's `trace/` folder), which removes grey and ground marks and keeps the drawing; (b) light-touch adapter sweep at 0.3 and 0.5 (runs 26 to 29), which keeps most base quality while flattening the shapes. Recommendation to Nick: base model plus threshold-and-trace as the main path; adapter at 0.3 optional; FLUX schnell as the base since he preferred its tiles.
- 2026-09-03 15:35: **LoRA v2 verdict.** The gentler recipe (lr 5e-5, 800 steps, style-worded captions, 512) fixed v1's damage: no broken tiles at full strength, and the set's vocabulary (flat black mass, thin white cut lines, white ground) carries into harness output. Thirstaserp control passes on my read (runs 24, 25). Frackworm improves (run 22) but the drill front still lands in about half the tiles and faces persist on some seeds; that is now a brief and seed-selection problem, not a style problem. Recommended next: adopt LoRA v2 at strength 0.7 to 1.0 as the default for Z-Image runs, generate 16 seeds per species and keep the sheet, then threshold and trace the picks.
- 2026-09-03 15:20: **LoRA v2 finished** (TRAIN EXIT 0, 800 steps from 13:58, about 4.7 s per step average, max GPU temperature 84 C during the burn-in and 77 C during training, no watchdog trigger, no crash). Final `xalsil_zimage_lora_v2.safetensors` (85 MB) plus checkpoints 100 to 700. Trainer samples with the full style prose stayed clean flat silhouettes throughout; the worm's front resolved from an eyed head (step 0) to a faceless stacked drill cone (steps 500 to 800), the ape from a shaded gorilla to a solid black mass with white tusks; no breakage at any step. Harness test runs 21 to 25 queued (`run-batch-0903e.sh`).
- 2026-09-03 13:35: **Training probe passed.** 100 steps at 3.6 s per step, max 80 C, 7.86 GB, 114 W, no watchdog trigger, no crash. Full LoRA v2 (800 steps, 512) started right after under the same telemetry and watchdog; expected about 50 minutes plus samples.
- 2026-09-03: **Third crash, 12:44, and a stop order.** Bugcheck 0x00020001 (HYPERVISOR_ERROR), minidump written. What the pipeline was doing at that moment: ai-toolkit LoRA v2 (512-only restart), past latent and caption caching, inside 'Generating baseline samples before training' (the trainer's own 768 px sampler) with VRAM at 7.86 GB of 8. The three crashes line up as follows: (1) 2026-09-02 21:05, 0x116 display driver timeout, during the first SDXL runs (the same evening as the 1024 px CUBLAS memory fault); (2) 2026-09-03 12:18, unclean reboot, two minutes into LoRA v2 at 7.7 GB; (3) 12:44 as above. What did not crash: LoRA v1 for 2 h 45 min at 7.7 GB, and every inference run (SDXL at 896, Z-Image, FLUX even at 10.5 GB spilled), so it is not deterministic, but every crash happened with the card at its 8 GB ceiling under the trainer or a memory-faulting SDXL run. **Standing rule from Nick (12:50): note what caused it and stop.** No more LoRA training on this laptop, and no run that sits at the VRAM ceiling, until the machine's stability is addressed (driver update, TDR timeout, power or clock cap, or a different machine for training). Inference runs that stay under about 6.5 GB (Z-Image 4-bit, SDXL 896) are the only GPU work allowed. Minidumps: C:/Windows/Minidump.
- 2026-09-03: **Two GPU-linked crashes in 16 hours.** Windows System log: 2026-09-02 21:05 bugcheck 0x116 (VIDEO_TDR_ERROR, the display driver stopped responding under load, minidump in C:/Windows/Minidump), and 2026-09-03 12:18 an unclean reboot with no bugcheck record. Both fall inside periods of full GPU load from this pipeline. Working assumption: the RTX 5070 laptop GPU or driver 595 is unstable under sustained load, likely thermal. Mitigations in force: 512-only training, keep VRAM below the 8 GB ceiling (avoid shared-memory spill), log GPU temperature per checkpoint. Not done without Nick: driver update, TDR registry timeout change, power or clock limits.
- 2026-09-03: **Machine crash 12:18** (Kernel-Power 41, unclean shutdown, no clean BSOD record) about two minutes into LoRA v2 training, with VRAM at the 8 GB ceiling and system RAM carrying the offloaded weights. Cause unproven; the hours of full GPU load before it are the obvious suspect (thermal or power). Mitigation on restart 12:35: v2 trains at 512 only (768 bucket dropped) to lower peak memory; watch GPU temperature in the log. All v1 outputs and checkpoints survived; v2 had no checkpoint yet.
- 2026-09-03: **LoRA v2 started 12:30** (`lora/xalsil-zimage-v2.yaml`, dataset `lora/dataset-v2` with captions that name the style, lr 5e-5, 800 steps, saves and samples every 100, sample prompts carry the full style prose). Base stays Z-Image: Nick preferred FLUX's base images, but FLUX training needs the 12B transformer in 8-bit (about 12 GB) plus a 31 GB ungated download, which does not fit 8 GB VRAM except by spilling into system memory; recorded as a possible later probe, not attempted.
- 2026-09-03: **Z-Image-Turbo on 8 GB works.** The pre-quantized 4-bit repo loads through `ZImagePipeline` with model CPU offload, 6.3 GB peak, 22 s per 1024 image, no memory faults, and the prose prompt is read in full (no 77-token limit). The drill appeared on the first sheet after 48 SDXL tiles without one.
- 2026-09-03: **Reference leakage is per reference.** Xylum's tentacles appeared as tendrils in most tiles of runs 7 and 8; thirstaserp's hood appeared once. Pick references for the anatomy you want copied, not for style alone, since the adapter copies both.
- 2026-09-03: Codex audit adopted: positive spatial prompt, artifact-only negative prompt, CPU RNG, manifest per image, gate on several species with a rubric, potrace over vtracer for holes, license manifest, art check is my own image reading.

## Next experiments (in order)

0. Pick track (current, 2026-09-04): Nick chose head 50000 + body 50004; run 53 composes them, 54 holds at 1536, traced SVGs sit well beside the set. Nick 10:35: depth is not there yet, wants the head at two thirds of the artwork and the body receding into the background. Runs 56 to 60: head crop alone as the reference gives the depth (57, 60); any whole tile locks the composition (56, 59). Run 61 was the depth pick set for the old design; superseded by the 11:00 ruling. Runs 62 and 63 done: eyes and slurry landed, drill lost to literal wording, set refs leak on the depth pose. Runs 64 and 65 done: 64010, 64015, 64007 carry drill, eyes, ports and slurry together. Runs 66 and 67 done: 66 is the depth pick set (66005, 66006, 66008, 66009, 66010). Nick 13:30: right direction; 64010 was a sloppy render so its crop made sloppy results; the 67011 head is the closest concept and needs cleanup; stalk eyes look goofy, back to pinpoint eyes. Runs 68 to 71 done; the clean 68003 crop gives clean, consistent sheets (70, 71) with no eyes. Run 72 put a faint eye dot on the cone base on half the seeds. Now: Nick judges the eye dot and names a seed from 70, 71 or 72; 1536 regeneration; trace; compare at size.
0q. (done) Showcase track: runs 38 to 51 done. Working recipe: klein, showcase brief (v3 Frackworm, v2 Thirstaserp), guidance 1, two references from the set that share no parts with the target. Next: Nick's read on 50 and 51 and his picks; trace the picks to SVG and compare against his set at size; a third species with hand-drawn art; split-and-splice (drill head and body as separate runs joined in Illustrator) is Nick's fallback, not yet taken up; then refine the Thirstaserp head toward cobra (flat wedge head, no brow ridge or ear frills) and the Frackworm drill cone (stacked spiral rings) and vents (a row on the neck); then a third species with hand-drawn art as a control; then trace the picks into SVG.
0p. (done) FLUX track: klein and schnell with pose variety (runs 34 to 37), then canon pass on Frackworm (size and menace from the record: 12 m, 3000 kg), then a third species with hand-drawn art as a control.
0v. Training paused by Nick 16:05. Main path now: base model (FLUX schnell preferred) plus threshold-and-trace; adapter at 0.3 optional. Model survey owed to Nick (see chat 16:10).
0w. (superseded) LoRA v2 adopted as the working adapter (pending Nick's read); next: 16-seed Frackworm sheet with v2 at 0.7, trace the picks, then a third species with hand-drawn art as a second control.
0x. (superseded 13:20 by Nick's driver and power changes; training allowed on the stepped path with telemetry) STOPPED: no training on this laptop (three crashes, see fixes list 12:44). LoRA v2 config and dataset-v2 are ready for a stable machine. Local work continues with inference only: threshold and trace on the best run 10 and 12 tiles, and FLUX brief iterations for Frackworm (drill head at the front).
0y. (superseded) LoRA v2 (next): lr 5e-5, 800 steps, style-worded captions, checkpoints every 100, harness test at 0.6 to 0.8 strength; decide Z-Image versus FLUX base with Nick.
0z. (done) LoRA training attempt started 2026-09-03 (log `C:\dev\src\xalians-art\lora\train-xalsil-zimage.log`, output `lora/output`): if it runs, judge the step-250/500/750 samples against the 29 originals; if it OOMs, retry at 512 only with rank 8, then consider `qtype` int4 or a FLUX LoRA on the 4-bit base is out of reach on 8 GB.
0c. Z-Image-Turbo track: if Nick accepts the register, next levers are pose and stylization variety (vary the brief per seed, add 'stylized, exaggerated proportions'), a threshold pass to remove grey, and a style LoRA on Nick's 29 silhouettes if the generic look is the remaining gap.
0b. (SDXL track, paused) Next Frackworm run: the drill must be described as the front of the body, not a part it has ('the worm's front end tapers into a spiral screw point, no head, no mouth, no eyes'), and 'head' should leave the brief since SDXL always draws a face for it. Consider a two-reference set of drilltail plus a worm-bodied species only, and guidance 7.
0a. Findings so far to carry: body brief in both encoders (kept); xylum leaks tendrils (dropped as a reference); adapter at 0.5 on all blocks holds style without copying anatomy; style-only blocks lose the species; the drill head has not appeared in any of 40 Frackworm tiles, so it is a reference or wording problem, not a seed problem.
1. Content leakage (partly covered by runs 4 and 5): rerun Frackworm with ip-scale 0.4, and separately with diffusers style-only attention targeting (`set_ip_adapter_scale` with a per-block dict that leaves content blocks at 0), references chosen without limbs (candidates: `xylum` for texture, `thirstaserp` alone), and "conical ringed drill point head" first in the prompt.
2. Known-species control: run `thirstaserp` with references `newtapede,xylum` (not itself) and compare against `docs/species-templates/art/thirstaserp.png` with the rubric, so the pipeline is scored against a ground truth.
3. If style holds and content follows the prompt: threshold plus potrace trial on the best tile, checked for compound holes, path count, black-only fills, against an existing SVG's structure.
4. If content still leaks: try the SDXL silhouette LoRA slider as an A/B, then consider FLUX schnell (gated, 24 GB) only with Nick's go.

## Open decisions for Nick

- Run 3 line quality: answered 2026-09-03, not acceptable yet (right style, not vector-like enough).
- Whether to invest in the known-species control before more Frackworm iterations (my recommendation: yes, one run).
