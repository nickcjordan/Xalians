{
  "name": "@xalians/content",
  "version": "1.0.0",
  "private": true,
  "description": "Shared Xalian game data (species, elements, moves, lore, planet records) consumed by both apps/api and apps/web. Single source of truth; nothing here is a copy.",
  "type": "module",
  "exports": {
    "./package.json": "./package.json",
    "./mock/*.json": "./json/mock/*.json",
    "./*.json": "./json/*.json",
    "./schema": "./src/schema/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "check:bundle": "node scripts/checkBundle.js"
  },
  "dependencies": {
    "zod": "^4.6.0"
  }
}
