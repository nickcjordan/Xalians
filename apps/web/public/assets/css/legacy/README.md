# Legacy stylesheets

Read only by the immersive pages (duel board and playground, Reclamation, training games, Long Return) until each gets its immersive brief, which deletes its share. `system.css` is the version 3 design system, `style.css` the old BootstrapMade template, `tokens.css` and `typeColors.css` the shims they need. Never add a rule here; never load these on a chrome page. Contract: docs/design/frontend-stack-migration.md, phase 4.
