{
  "name": "xalians-lambda",
  "version": "1.0.0",
  "private": true,
  "description": "Xalian generation engine and CRUD lambdas. Dependencies here are installed into lambda/node_modules and shipped in the deployment zip; the AWS SDK is not among them because nodejs20.x provides @aws-sdk/* at runtime.",
  "dependencies": {
    "uuid": "^8.3.2"
  }
}
